# VoiceClaw Evolution Pipeline — Python Dependencies
# Install into a dedicated venv: scripts/evolution-venv/
#
# Setup: python -m venv scripts/evolution-venv
#        scripts/evolution-venv/Scripts/activate  (Windows)
#        pip install -r scripts/requirements-evolution.txt

unsloth[cu121-ampere-torch260]
torch>=2.6.0
datasets>=3.0.0
trl>=0.12.0
transformers>=4.45.0
peft>=0.14.0
bitsandbytes>=0.45.0
accelerate>=1.0.0
